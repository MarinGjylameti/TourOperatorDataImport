#!/bin/bash
# wait-for-sql.sh

set -e

host="$1"
shift
cmd="$@"

until /opt/mssql-tools/bin/sqlcmd -S "$host" -U sa -P "YourPassword123!" -Q "SELECT 1;" &> /dev/null; do
  echo "SQL Server is unavailable - sleeping"
  sleep 1
done

echo "SQL Server is up - executing command"
exec $cmd